# 45Drives Firmware Repository Configuration
#
# This file configures where firmware-check and firmware-flash download
# manifests, firmware files, and flash tools from.
#
# REPO_URL - Base URL for firmware file and tool downloads
# MANIFEST_URL - URL to the firmware manifest JSON (with .sig alongside)
# VERIFY_GPG - Whether to verify GPG signature on remote manifests (true/false)
#              Set to false only for internal/development repos.

REPO_URL=http://192.168.209.126:8090
MANIFEST_URL=http://192.168.209.126:8090/manifest.json
VERIFY_GPG=false
